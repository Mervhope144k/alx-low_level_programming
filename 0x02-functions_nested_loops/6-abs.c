#include "main.h"
/**
 * _abs - compute for absolute value of an integer
 * @n: The parameter to compute for absolute value
 * Return: positive value equal to n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
