#include <stdio.h>

/**
 * main - function that will complete the task
 * @argc: argument1 to main but it is unusd in this program
 * @argv: argument2 to main, a pointer to a string
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
